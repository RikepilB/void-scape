# Selected RSS resource reads

Issue #57 continuation. User authorized engineering, tests, documentation and PRs.

- Resolve a single verified entry's original unredacted resource URL.
- Keep selection read-only; distinguish article and explicitly typed enclosures.
- Bind approved public article inspect/preview/read to the retained capture.
- Verify source, article shape, bounded artifact hashes and citations before notes.
- Preserve local defaults, independent fetch approval, timeout ownership and skips.

Validation: real article parser/manifest tests with synthetic network payloads;
gate refusal, wrong resource, mutation, resume and publication checks; a bounded
public article CLI run; full suite and hosted checks. No private account evidence.

This implementation addresses article reads. Remaining full-goal work includes
bounded enclosure acquisition/media receipts, observed paywall disposition, and
independent representative skill/harness evaluation. Do not close #57 from this
slice or claim provider/browser adoption.
