# Verified RSS note publication

Advance issue57 from captured entries to analyzed notes using triage_store.
Keep RSS capture and note completion separate; no model/cloud/account actions.

1. Add an explicit RSS source validator bound to a verified capture root and
   canonical RSS key. Frontmatter must match retained URL, author and date.
2. Retain capture entry and marker as required evidence. Require RSS note
   sections and a short verbatim untrusted excerpt, plus the existing priority,
   title, artifact hash, receipt, index and no-overwrite checks.
3. Deduplicate RSS publication by verified canonical key under the same lock.
   Skipped records remain distinct and may later become analyzed notes.
4. Test provenance mismatch, changed capture evidence, publication recovery,
   duplicate notes, skipped-to-analyzed transition and legacy Instagram behavior.
5. Validate one public-feed note locally and document actual scope. Source skill,
   scheduling/media routing and independent harness acceptance remain separate.
