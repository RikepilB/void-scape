# Substack/RSS project skill

Build the issue57 source skill on merged RSS capture and note publication.
Add a read-only retained inventory so capture dedup cannot hide unfinished notes.
Revalidate capture and note state, filter a selected publication, expose incomplete
capture/pending publication, and keep local reads bounded and nonmutating.

Author the project skill with actual CLI/schema guidance; generate Claude/Agents/
Codex entry points and register a private project-owned library snapshot. Keep
publication roots explicit, content untrusted, fetch/write permissions separate,
and incomplete/skip/error states distinct from verified analysis.

Verify helper state transitions, no-write inventory, entry integrity, feed scope,
CLI modes and generated packaging. Record realistic evaluation cases without
claiming independent actor or harness parity. Full issue57 still requires those
evaluations, complete source/media routing and the YouTube workflow.
