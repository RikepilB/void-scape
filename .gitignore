# --- Personal machine config (absolute paths to YOUR vault) — never commit ---
skill/workspace.json
workspace.json

# --- Local media + generated transcripts (private content) ---
samples/*
!samples/.gitkeep
*.mp4
# demo recording shipped with the docs (guide embed + README link)
!docs/assets/cli-demo.mp4
*.mov
*.mkv
*.webm
*.wav
*.m4a
*.mp3
*.srt
*.vtt

# --- Secrets ---
.env
.env.*
!.env.example
*.key
*.pem
*cookies*.txt
.foglamp/

# --- Local user-research responses (may contain participant feedback) ---
/research/responses/
*survey-responses*.json
*survey-responses*.csv

# --- Session/process scratch (never repo content) ---
20[0-9][0-9]-[01][0-9]-[0-3][0-9]-*-this-session-is-*.txt
task-*-brief.md
task-*-report.md
# Private agent handoff/session artifacts — never publish.
/handoff.md
/docs/handoff/
# Reversible local plugin build staging and backups.
/plugins/voidscape/skills/.plugin-stage-*/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
build/
dist/
*.egg-info/


# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
